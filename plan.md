1. Add an empty state with helpful call-to-action when there are no songs in the library.
   - Currently, it just says `No songs in library` in a `div`. I will enhance this to include a `Music` icon and a more user-friendly message, potentially with a prompt to click the `+` button to add a song.
2. Fix missing `aria-label` or `id` on the `<textarea>` in the Advanced Mode: JSON Live Buffer in `src/App.tsx`.
3. Complete pre commit steps
   - Complete pre commit steps to make sure proper testing, verifications, reviews and reflections are done.
4. Submit the change.
   - Once all tests pass, I will submit the change with a descriptive commit message.
